# Supabase Notes

This folder stores SQL and future Supabase-related assets for the Chomoe Production Team CRM project.

Files:

- `schema.sql`: initial tables, seed data, and development RLS policies

Use `schema.sql` inside the Supabase SQL editor when bootstrapping the database.
